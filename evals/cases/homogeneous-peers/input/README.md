# Locale catalog

One team owns all locale files. Every file is a single translation table with the same schema and release lifecycle. The loader resolves the locale identifier directly; consumers never reference file paths.
