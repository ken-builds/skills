interface:
  display_name: "Implementation Rationale"
  short_description: "Document public APIs and implementation rationale"
  default_prompt: "Use $implementation-rationale to audit public API comments and preserve implementation rationale in source, tests, and records."
policy:
  allow_implicit_invocation: true
